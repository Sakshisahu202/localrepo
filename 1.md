1. What command is used to display file permissions in Linux?
2. Explain the meaning of `r`, `w`, and `x` in file permissions.
3. What are the three categories of users in Linux permissions?
4. What does the command `chmod 644 file.txt` do?
5. In the output `-rw-r--r--`, what permissions does the group have?
6. How do you add execute permission to the owner of a file?
7. Write the command to remove write permission for others.
8. What is the difference between `chmod 777` and `chmod 755`?
9. Which command is used to change the owner of a file?
10. Which command is used to change the group of a file?

**Medium Level**
11\. What does `chmod u+x,g-w file.sh` do?
12\. Explain the difference between symbolic mode and numeric mode in `chmod`.
13\. How do you give read and write permission to all users using `chmod`?
14\. In permission string `drwxr-x---`, what does the leading `d` mean?
15\. What happens if a directory doesn’t have `x` permission?
16\. How do you recursively change permissions for all files in a directory?
17\. What is the effect of `chmod 000 file.txt`?
18\. How do you set permissions so only the owner can read and write, and nobody else can access?
19\. Explain the purpose of the `umask` command.
20\. What is the difference between `chown user file` and `chown user:group file`?

---

